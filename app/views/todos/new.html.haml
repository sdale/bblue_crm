.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "List", todos_path
      %li.active= link_to "New To-Do", new_todo_path
  .content
    %h2.title= "New To-Do"
    .inner
      - form_for :todo, :url => todos_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}

